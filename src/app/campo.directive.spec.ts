import { CampoDirective } from './campo.directive';

describe('CampoDirective', () => {
  it('should create an instance', () => {
    const directive = new CampoDirective();
    expect(directive).toBeTruthy();
  });
});
